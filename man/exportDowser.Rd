% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportClones.R
\name{exportDowser}
\alias{exportDowser}
\title{Export scRepertoire Clones for dowser / Immcantation}
\usage{
exportDowser(
  input.data,
  group.by = NULL,
  clone.call = "CTstrict",
  run.formatClones = FALSE,
  write.file = FALSE,
  dir = NULL,
  file.name = "dowser_clones.csv",
  ...
)
}
\arguments{
\item{input.data}{The product of \code{combineTCR()}, \code{combineBCR()}, or
\code{combineExpression()}.}

\item{group.by}{The variable to use for grouping. If \code{NULL}, data are
grouped by sample.}

\item{clone.call}{The clone definition column mapped to dowser's \code{clone_id}.
Defaults to \code{"CTstrict"}.}

\item{run.formatClones}{If \code{TRUE}, calls \code{dowser::formatClones()} on the
assembled data frame and returns its result (requires the \code{dowser}
package). If \code{FALSE} (default), returns the AIRR data frame.}

\item{write.file}{If \code{TRUE}, writes the AIRR data frame to a CSV file.}

\item{dir}{The directory for the output file. Defaults to the working
directory.}

\item{file.name}{The name of the output file.}

\item{...}{Additional arguments passed to \code{dowser::formatClones()} when
\code{run.formatClones = TRUE}.}
}
\value{
An AIRR-compliant data frame (default) or, when
\code{run.formatClones = TRUE}, the object returned by
\code{dowser::formatClones()}.
}
\description{
Reshapes a \code{combineTCR()}, \code{combineBCR()}, or \code{combineExpression()} object
into an AIRR-compliant data frame ready for \code{dowser::formatClones()}, the
entry point for B cell lineage / phylogenetic analysis in the Immcantation
framework.
}
\details{
dowser builds lineage trees from full-length, IMGT-gapped sequences, so the
input must carry \code{sequence_alignment} and \code{germline_alignment}. These come
from an Immcantation/AIRR annotation pipeline (IgBLAST + MakeDb +
CreateGermlines) and must be retained through the combine step, e.g.
\code{combineBCR(..., retain.sequences = c("sequence_alignment", "germline_alignment"))}.
Raw 10x Cell Ranger output does not contain these fields, and the function
errors with guidance when they are missing.
}
\examples{
\dontrun{
# BCR data imported from an Immcantation pipeline, with alignments retained
combined <- combineBCR(immcantation_contigs,
                       retain.sequences = c("sequence_alignment",
                                            "germline_alignment"))
airr <- exportDowser(combined, write.file = FALSE)
clones <- dowser::formatClones(airr)
}
}
\concept{Loading_and_Processing_Contigs}
